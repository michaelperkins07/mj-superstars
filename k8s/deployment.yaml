# ============================================================
# Top Performer - Kubernetes Deployment
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: top-performer-api
  namespace: top-performer
  labels:
    app: top-performer
    component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: top-performer
      component: api
  template:
    metadata:
      labels:
        app: top-performer
        component: api
    spec:
      containers:
        - name: api
          image: ghcr.io/your-org/top-performer/backend:latest
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3001"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: tp-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: tp-secrets
                  key: jwt-secret
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: tp-secrets
                  key: encryption-key
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: tp-secrets
                  key: anthropic-api-key
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: top-performer-api
  namespace: top-performer
spec:
  selector:
    app: top-performer
    component: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: top-performer-ingress
  namespace: top-performer
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - api.topperformer.app
      secretName: tp-tls
  rules:
    - host: api.topperformer.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: top-performer-api
                port:
                  number: 80
